{
  "name": "h-vard-farm",
  "private": true,
  "description": "H-vard Farm monorepo — agriculture app (app/) + event venue site (website/)",
  "scripts": {
    "build": "npm ci --prefix website && npm ci --prefix app",
    "start": "npm start --prefix app",
    "start:app": "npm start --prefix app",
    "start:website": "npm start --prefix website"
  },
  "engines": {
    "node": ">=18"
  }
}
